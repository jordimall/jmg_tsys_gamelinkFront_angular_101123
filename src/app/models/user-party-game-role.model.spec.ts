import { UserPartyGameRole } from './user-party-game-role.model';

describe('UserPartyGameRole', () => {
  it('should create an instance', () => {
    expect(new UserPartyGameRole()).toBeTruthy();
  });
});
