import { ValidatorFormControl } from './validator-form-control';

describe('ValidatorFormControl', () => {
  it('should create an instance', () => {
    expect(new ValidatorFormControl()).toBeTruthy();
  });
});
